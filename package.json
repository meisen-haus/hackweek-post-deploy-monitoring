{
  "name": "hackweek-post-deploy-monitoring",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke": "playwright test"
  },
  "dependencies": {
    "@sentry/browser": "^10.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
